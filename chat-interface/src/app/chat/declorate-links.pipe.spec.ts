import { DeclorateLinksPipe } from './declorate-links.pipe';

describe('DeclorateLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new DeclorateLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
